import React, { FC } from 'react';
import SearchForm from '../SearchForm/SearchForm';

const Header: FC = () => {
    return (
        <header>
            <h2>Header</h2>
        </header>
    );
};

export default Header;